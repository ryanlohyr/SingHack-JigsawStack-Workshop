[project]
name = "singhack-agent"
version = "1.0.0"
description = "RegTech Compliance Agent - Python version"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.54.0",
    "python-dotenv>=1.0.0",
    "pydantic>=2.9.0",
]

[tool.uv]
dev-dependencies = []

